{
    "require": {
        "symfony/flex": "^1.1",
        "symfony/requirements-checker": "^1.1"
    },
    "scripts": {
        "auto-scripts": {
            "requirements-checker": "script"
        }
    }
}
